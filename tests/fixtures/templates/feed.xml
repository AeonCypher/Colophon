<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
  <channel>
    <title>{{ site.title | e }}</title>
    <link>{{ site.url | e }}</link>
    <description>{{ site.description | e }}</description>
    <lastBuildDate>{{ build_date.strftime("%a, %d %b %Y %H:%M:%S +0000") }}</lastBuildDate>
    {% for post in posts %}
    <item>
      <title>{{ post.title | e }}</title>
      <link>{{ public_url(post.url) | e }}</link>
      <guid>{{ public_url(post.url) | e }}</guid>
      <pubDate>{{ post.date.strftime("%a, %d %b %Y 00:00:00 +0000") }}</pubDate>
      <description>{{ post.summary | e }}</description>
    </item>
    {% endfor %}
  </channel>
</rss>
