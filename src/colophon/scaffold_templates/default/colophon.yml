paths:
  content: content
  templates: templates
  static: static
  output: _site
  deploy: content/deploy.example.yaml
python:
  modules:
    - site_hooks.py
vendor:
  mode: auto
  local_dir: vendor
  assets:
    webawesome:
      enabled: false
