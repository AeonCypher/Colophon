deploy:
  default_target: production
  steps:
    - preflight_build
    - build
    - upload
  targets:
    production:
      transport: ftps
      host: example.test
      username: deploy
      password: env::EXAMPLE_FTP_PASSWORD
      remote_path: public_html/example.test/
      purge: true
